### 一些习题

* 输入一个正整数，判断它有多少位
```c
#include <stdio.h>

int main()
{
    int x = 0;
    int n = 0;
    
    printf("请输入一个正整数：");
    scanf("%d", &x);
    
    while (x > 0) {
        n++;
        x /= 10;
    }
    printf("您输入的是一个%d位数", n);
    
    return 0;
}
```
